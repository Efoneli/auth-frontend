/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['storage.googleapis.com', 'upload.wikimedia.org', 'encrypted-tbn0.gstatic.com', 'miro.medium.com'],
      },
};

export default nextConfig;
